# Mini KMeans Clustering

An implementation of kmeans algorithm for small matrices and dimensions. Built to test aie kernels which do not implement batch streaming. They can only handle matrices which can fit inside one core at a time.

